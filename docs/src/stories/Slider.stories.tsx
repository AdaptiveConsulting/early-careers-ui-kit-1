import Slider from "components/src/Slider"
import { Meta } from "@storybook/react"

const meta: Meta = {
  title: "Slider",
  component: Slider,
}

export default meta

export const Default = () => <Slider />
