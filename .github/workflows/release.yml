name: Release

on:
  push:
    branches: ["**"]
    # type: [created]
  # permissions:
  #   contents: write
  #   issues: write
  #   pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - env:
        # RELEASE_VERSION: ${{ github.event.release.tag_name }}
        RELEASE_VERSION: 0.0.3
      - name: Test changelog
        run: echo $RELEASE_VERSION | yarn version
